package Frame;

import java.awt.*;

public class Pipe extends Obstacle{
    public Pipe(String name, int x, int y, int wide, int high, Image image) {
        super(name, x, y, wide, high, image);
    }
}
